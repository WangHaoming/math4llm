# Claude Code 工作日志

## 2026-07-11

### 初始理解
- 用户要求阅读 `demos/dot-product/` 目录,并在其 `README.md` 中:
  1. 详细讲解点积的计算原理与几何意义。
  2. 结合 `index.html` 的 demo(拖动 a、b 两向量,显示投影/夹角/平行四边形)讲解。
  3. 重点讲清「为什么点积对神经网络的实现最重要」。

### 变更计划
- 仅修改 `demos/dot-product/README.md`,不动代码(demo 功能已满足讲解需要)。
- 强化「计算原理」:代数定义与几何定义为何等价(投影 / 余弦定理)。
- 结合 demo 各控件(投影黄色箭头、夹角、平行四边形)落地几何意义。
- 新增一节「为什么点积对神经网络最重要」:神经元 = 加权求和 = 点积;
  相似度 / 注意力;矩阵乘法本质是大量点积;GPU 并行。

### 总结
- 已重写 `demos/dot-product/README.md`,新增三大板块:
  一、计算原理(用余弦定理证明代数=几何两定义等价,并给出由点积反解长度/夹角/投影)。
  二、几何意义(在原有基础上补充点积的对称性与三因素:方向一致度 + 两者长度)。
  三、为什么点积对神经网络最重要(神经元=点积、点积=相似度打分、层=矩阵乘法=一批点积、
      硬件 MAC/GPU 并行、反向传播复用)。
- 全程结合 demo 控件(黄色投影箭头、夹角、平行四边形、面板各行)讲解。
- 仅改动文档,未改代码;未运行测试(纯文档变更,无需)。

---

## 2026-07-14 · what-is-nn:增加 USE_RELU 开关

**初始理解**:用户想做消融实验——看关闭 ReLU 后识别准确率的变化。训练与推理共用
`demos/what-is-nn/index.html` 里的 `forward()`;ReLU 出现在两处:前向的截断
(`z > 0 ? z : 0`)和反向的梯度掩码(`h <= 0` 时跳过)。不上 UI,用 const 控制。

**改动计划**:
1. 模型段顶部加 `const USE_RELU = true;`,注释说明改 false 即可做无激活函数对照实验。
2. 前向:`USE_RELU=false` 时隐藏层直接输出线性值 z。
3. 反向:掩码条件加上 `USE_RELU &&`——线性模式下负激活也要传梯度,否则反向就错了。

**总结**:在 `demos/what-is-nn/index.html` 加了 `const USE_RELU = true`(第 397 行附近),
前向截断与反向掩码均受其控制;`forward()` 为训练/推理共用,故一处开关两边生效。
未运行自动化测试(纯前端 demo,需浏览器里手动训练验证)。已知小瑕疵:线性模式下
隐藏层热力图对负激活显示为空白,仅影响可视化,不影响准确率。

---

## 2026-07-15 · dot-product:新增讲解长文 basic.md

**初始理解**:用户要一篇解释点积的文章,要求从 cos 的几何意义讲起,重点讲点积在
神经网络中的几何意义,并加入图解。现有 README.md 有两个缺口:cos 被空降、NN 节
讲工程不讲几何。已与用户确认结构。

**改动计划**:
1. 新建 `demos/dot-product/basic.md`,风格对齐 `what-is-nn/basic.md`
   (中文长文、〇一二三分节、引用块放关键结论、ASCII 字符画做图解)。
2. 叙事线:cos = 方向一致度的尺子 → 点积 = 影子×长度(保留余弦定理证明)
   → 神经元的几何(w 是方向、激活是投影、超平面、ReLU)→ 高维(embedding/注意力)
   → 回到 demo。
3. README.md 顶部加一行指向 basic.md 的链接。

**设计决策**:图解用 ASCII 字符画而非 SVG/图片——与 what-is-nn/basic.md 先例一致,
且纯文本在任何 markdown 查看器里都能显示,符合项目"无构建"约定。

**总结**:新建 `demos/dot-product/basic.md`(约 300 行长文,含 6 幅 ASCII 图解:
单位圆影子、cos 尺子、投影、符号表、决策边界/超平面等);README.md 顶部加了指向
basic.md 的链接。未运行测试(纯 markdown 文档,无可执行代码)。

---

## 2026-07-15 · dot-product:basic.md 合并进 README.md

**初始理解**:用户定下约定——每个 demo 只能有一个 README.md 说明(讲解)文档。
需删除昨天新建的 dot-product/basic.md,内容并入 README.md。

**改动与总结**:
1. 重写 `demos/dot-product/README.md`:保留「如何使用」;原「向量加法」一节
   作为热身(第一节)插入长文叙事线;其余讲解整体替换为长文内容(cos 尺子 →
   影子×长度 → 神经元几何 → 高维),重编章节号并修正内部交叉引用;
   原 README 的工程向内容(GPU/MAC)压缩为一句话,「通向最小二乘」并入结尾。
2. 删除 `demos/dot-product/basic.md`。
3. `CLAUDE.md` 硬性约定第 1 条补充"只能有一个说明文档"。
注:`demos/what-is-nn/basic.md` 同样违反新约定(且被其 index.html 注释引用),
待用户确认后另行处理。未运行测试(纯文档改动)。

---

## 2026-07-15 · dot-product README:第三节重构(计算规则前置 + sinθ 证明)

**初始理解**:用户要求①先讲点积计算规则再讲几何意义;②不用余弦定理,改用
"把坐标轴转到与 b 对齐,a 拆成 (|a|cosθ, |a|sinθ),代入乘加规则直接算"的证明。

**改动与总结**:第三节重排为 3.1 计算规则(含数值例子)→ 3.2 几何意义(影子×长度,
改为"声明"口吻)→ 3.3 新证明(转地面 + sinθ 项乘零点题"点积无视垂直成分",
旁注用差角公式说明乘加的旋转不变性)→ 3.4 符号速查表。已验证:全文无"余弦定理"
残留,小节编号连续,交叉引用成立。保留了用户手动加的第一节 Transformer 注记与
2.1 措辞。未运行测试(纯文档改动)。

---

## 2026-07-15 · dot-product README:公式改为标准 LaTeX 写法

**初始理解**:用户指出 3.1 的公式用 Unicode 下标拼凑(aₓ 与 a_y 混杂)不规范,
要求用标准公式。

**改动与总结**:全文独立成行的公式统一改为 `$$...$$` LaTeX 显示公式(GitHub /
主流 markdown 预览可渲染),关键结论引用块内改用 `$...$` 行内公式;3.1 顺带补了
n 维求和式 Σaᵢbᵢ。ASCII 图解内的标注保持字符画。已验证无 Unicode 下标残留。
未运行测试(纯文档改动)。

---

## 2026-07-15 · dot-product:README 补 sinθ 说明 + demo 新增「证明演示」

**初始理解**:用户要求①README 里补充 sinθ 的说明,为 3.3 的坐标拆分证明铺垫;
②demo 中实现证明的图像演示:任意两个向量,夹角固定,把 b 转到 x 轴上,
在坐标系上标注各步计算(a·b = p×|b| = |a||b|cosθ)。

**改动计划**:
1. `shared/math-canvas.js`:新增通用 `label()`(在数学坐标处画文字标注)——
   按约定通用绘图能力进公共层。
2. `demos/dot-product/sketch.js`:新增证明演示模式。勾选后整个图形刚性旋转
   −β(β 为 b 的方位角),动画平滑过渡;b 躺平后标注影子 |a|cosθ(黄)、
   举高 |a|sinθ(白虚线)、b=(|b|,0)、θ 圆弧;原始 a、b 淡显留作参照,
   仍可拖动;演示期间隐藏平行四边形/投影图层避免混乱。
3. `demos/dot-product/index.html`:加「证明演示」开关与步骤面板
   (躺平后 b、此时 a、乘加算式、结果 = 转轴前的 a·b,强调旋转不变)。
4. README:2.1 图解标注 sinθ 并补"一横一竖两个成分 + 勾股"一段;
   「如何使用」加第三个开关;3.3 加指向演示开关的引导。

**总结**:
- `shared/math-canvas.js` 新增通用 `label()`;`sketch.js` 新增证明演示
  (刚性旋转动画 + 影子/举高/θ 圆弧/坐标标注 + 步骤面板,面板数值按目标坐标
  计算保证动画中稳定);`index.html` 加开关与 proof-box;README 补 sinθ
  一段(勾股 + 横竖分解)并更新用法与 3.3 引导。
- **已用真实 Chrome 验证**(puppeteer-core 驱动 headless Chrome +
  python http.server):勾选后动画完成,b=(2.24,0)、a=(0.45,3.13)、
  乘加 0.45×2.24+3.13×0 = 1.00,与转轴前点积一致;取消勾选正常转回;
  截图确认渲染无误。修了一处截图发现的标签重叠(影子与 b 的标注分行错开)。
- 控制台唯一 404 为 /favicon.ico(项目本来就没有 favicon,与本次改动无关)。

---

## 2026-07-15 · 证明演示:b 的画布标注改为符号形式

**改动与总结**:`demos/dot-product/sketch.js` 里 b 躺平后的画布标注由具体数值
`b = (2.24, 0)` 改为符号形式 `b = (|b|, 0)`,与 a 的符号标注及 README 3.3
证明步骤一一对应;具体数值仍在面板「躺平后 b」显示。影子/举高标签保持
「符号 = 数值」形式(对应面板乘加算式的两个乘数)。已用 headless Chrome
重新验证:截图确认新标注渲染正确、无重叠,面板数值不变,控制台除既有
favicon 404 外无报错。

---

## 2026-07-15 · dot-product:全屏布局,画布随窗口伸缩

**初始理解**:用户要求 demo UI 占满整屏,右侧 panel 宽度不变(340px),
左侧坐标系画布随屏幕大小变化。已确认 what-is-nn 不引用 shared 的 .demo
布局,改 shared/style.css 无副作用。

**改动计划**:①style.css:.demo 高 100vh 不换行、.canvas-holder flex:1、
.panel 固定 340px 可内部滚动、加窄屏纵排回退;②math-canvas.js:MathViz
支持 w/h 矩形尺寸(size 向后兼容)与 resize() 方法,toScreen/toLocal/
drawGrid 全部改用 w/h;③sketch.js:按 canvas-holder 实际尺寸建画布,
新增 windowResized() 联动 resizeCanvas + viz.resize。

**总结**:
- `shared/style.css`:.demo 改为 100vh 全屏 flex(stretch、不换行),
  .canvas-holder flex:1 占满剩余空间,.panel 固定 340px、超高内部滚动,
  另加 ≤800px 窄屏纵排回退。
- `shared/math-canvas.js`:MathViz 支持 w/h 矩形尺寸(size 向后兼容)与
  resize() 方法;toScreen/toLocal/drawGrid 全部改用 w/h,原点始终居中。
- `demos/dot-product/sketch.js`:画布按 #canvas-holder 实际尺寸创建;
  用 ResizeObserver 盯容器(首测发现 KaTeX 渲染前后面板宽度变化不触发
  windowResized,导致初始画布 664px < 容器 788px,改 Observer 后修复)。
- **已用 headless Chrome 验证**:1200×800 与 1680×1000 下画布均与容器
  等大、面板恒 340px;动态改视口画布跟随;resize 后证明演示功能正常
  (乘加 = 转轴前点积 1.00);控制台除既有 favicon 404 无报错。

---

## 2026-07-15 · dot-product:「显示平行四边形 a + b」默认关闭

**改动**:index.html 中 #showSum 复选框去掉 checked 属性,页面打开时默认
只显示投影,减少初始画面元素;勾选后功能不变。纯 HTML 属性改动,未运行测试。

---

## 2026-07-15 · what-is-nn/README.md:公式改为标准 LaTeX 写法

**改动与总结**:与 dot-product README 同一标准——独立公式用 $$...$$,行内数学量
用 $...$,中文概念词以 \text{} 保留。共转换:招聘官总分、神经元得分(196 项
Unicode 下标)、投票总分(64 项)、点积几何式 w·x=|w||x|cosθ、两处更新正比
关系(∝ 输入×责任 / ΔW1 ∝ 误差×输入)、Q1 旋钮变化量、Q4 交叉熵(−ln p、
e^{−L} 三处、ln 10)、Q6 的 1/√B。只改排版未动文字内容;已 grep 验证无
Unicode 下标/旧写法残留。未运行测试(纯文档改动)。

---

## 2026-07-15 · dot-product README:字符画改为跨平台安全的纯 ASCII

**初始理解**:用户在 Chromebook 上看到图解斜线错位。根因:`╱│─┆●┴▶←` 等
字符属 Unicode"东亚宽度歧义"类,西文等宽字体渲染半角、CJK 字体渲染全角,
不同平台字体回退不同;行中混排中文也同理。

**改动计划**:①重画 README 5 处图解——对齐列只用纯 ASCII(/ | - + * . < > ^ =),
中文/θ/° 只放行尾标注区;cos 尺子改为 ASCII 单行 + markdown 表格;
②CLAUDE.md 硬性约定补一条字符画规范;③python 脚本机械校验
"非 ASCII 字符之后不再出现对齐字符"。

---

## 2026-07-15 · dot-product README §4.1:补讲"× 长度"的意义

**初始理解**:用户指出 4.1 讲神经元打分只解释了投影(方向)部分,没讲
"乘长度"的意义。确认属实:w·x = cosθ × |x| × |w| 三因子,后两个未交代。

**改动与总结**:4.1 末尾(模板引用块后、4.2 前)插入「那"× 长度"去哪了?」
补充块:三因子拆解公式(underbrace 标注)+ 三点——影子藏着 |x|(输入力度,
浓墨=强证据)、|w| 是模板音量(训练既转方向也调长度,呼应 what-is-nn 热图
深浅)、不想要长度时除掉→余弦相似度(为 5.1 埋伏笔);引用块收束三因子
一句话。未改小节编号与交叉引用;顺手修掉 4.1 里链接后多余的"k"。
未运行测试(纯文档改动)。

**补充**:三张图 θ 行末尾的虚线点已按"θ 后不留对齐字符"原则收掉(借虚线断格),
校验脚本对 dot-product 与 what-is-nn 两个 README 均通过。

---

## 2026-07-15 · dot-product demo:坐标轴加刻度数字

**初始理解**:用户要求画布坐标系标出 x、y 轴刻度。属通用绘图能力,按约定
加入公共层 MathViz。

**改动计划**:①`shared/math-canvas.js` 新增 `drawTicks()`:x 轴刻度数字标在
轴下方、y 轴标在轴左侧,每 1 数学单位一个,0 只在原点标一次,颜色用面板
muted 灰;②`demos/dot-product/sketch.js` 在 drawGrid 后调用;③浏览器截图验证。

**总结**:shared/math-canvas.js 新增 drawTicks()(x 轴数字标轴下、y 轴标轴左,
每 1 单位一个,0 只标原点,近边缘不画,muted 灰 10px);sketch.js 在 drawGrid
后调用。已用 headless Chrome 截图验证:两轴 −9…9 刻度清晰、原点 0 正确、
全屏布局与向量绘制不受影响;控制台除既有 favicon 404 无报错。

---

## 2026-07-16 · dot-product README:统一"打分 = 投影 × |w|"口径

**初始理解**:用户指出〇结论与 4.1 的口号句仍说"打分就是投影/问影子有多长",
与 4.1 的"× 长度"块矛盾。

**改动与总结**:保留"影子有多长"记忆钩子,4 处口号补第二拍"再乘上模式/
模板自身的分量(音量 |w|)":〇结论引用块、4.1 锚点引用块(修正块内前后
不一)、4.1 模板句("打分就是投影 × 模板的音量 |w|")、六收拢第 3 条;
"× 长度"块开头衔接句同步微调。已 grep 确认无"打分=纯投影"孤立表述。
未运行测试(纯文档改动)。

---

## 2026-07-16 · dot-product README §4.1:面试官打分类比三因子

**初始理解**:用户要求用面试打分的例子形象说明"打分 = 方向 × 输入力度 ×
模板音量";用户在三因子引用块后已起头未写完("x 的分量 = 考核项")。

**改动与总结**:顺着用户起头补完:先接分量视角(考核项 × 心中分值,逐项
乘加 = 3.1 规则),再展开几何三问——方向 = 对不对口(配比像不像画像)、
|x| = 底子多厚(方向×力度=影子,对口的本事有多深)、|w| = 面试官话语权
(训练 = 公司复盘调话语权,报给录用委员会 = 下一层);一句话收束。
呼应 what-is-nn 招聘官叙事。未运行测试(纯文档改动)。

---

## 2026-07-16 · 新 demo:最小二乘法(demos/least-squares/)

**初始理解**:按首页规划做「最小二乘法」demo——散点拟合直线,看残差平方和
如何被最小化。按 CLAUDE.md 新 demo 流程:复制 dot-product 模板、基于 MathViz、
写 README、激活首页卡片。

**设计**:
- 画布:可拖动散点(初始 8 个,点空白处可加点,上限 15);一条由两个端点
  手柄定义的可拖动直线;开关控制显示残差(竖直虚线)与残差平方(以残差为
  边长的半透明正方形);灰色参考线显示闭式解最佳拟合。
- 面板:当前 k、b、残差平方和 SSE;最优 k*、b*、最小 SSE;KaTeX 公式。
- 「一键拟合(看下山)」按钮:对 (k,b) 跑梯度下降动画,SSE 实时下降——
  与 what-is-nn 的"下山"叙事衔接;「重置数据」恢复初始散点。
- 复用 MathViz(网格/刻度/坐标变换/拖拽),直线/正方形绘制属 demo 特有,
  留在 sketch.js。README 遵守纯 ASCII 字符画约定。
- 收尾:激活首页卡片,CLAUDE.md 规划列表更新,headless Chrome 验证。

**总结**:
- 新增 demos/least-squares/(index.html + sketch.js + README.md);
  shared/math-canvas.js 补 removeHandle()/isDragging(),shared/style.css 补
  .panel button 通用样式;首页卡片激活;CLAUDE.md 规划列表更新为 ✅。
- 功能:可拖散点(点击加点/双击删点)、双端点可拖直线、残差虚线、
  残差平方黄色正方形(面积=SSE 贡献)、闭式解参考线、梯度下降拟合动画
  (过均值点参数化,k/c 解耦不绕弯)、面板实时 k/b/SSE 与 k*/b*/minSSE。
- README:残差与平方的理由 → 闭式解与梯度下降(对照 what-is-nn 表格)→
  投影几何(回收点积伏笔、正规方程)→ 试试情形(离群点/两点过拟合/
  竖线无解/无局部最优)→ 通向 MSE/线性探针。ASCII 校验通过。
- **headless Chrome 端到端验证**:一键拟合 SSE 50.94→2.18(= 最小 SSE,
  k/b 收敛到 k*/b*);点击加点 8→9;双击删点 9→8(测试工具不合成 dblclick,
  改用派发标准 DOM 事件验证通过);重置正常;截图确认视觉;修掉面板公式
  过长被裁的问题。控制台除 favicon 404 无报错。

---

## 2026-07-16 · least-squares README:补「下山与投影是同一件事」小节

**改动与总结**:第三节末尾新增「### 下山与投影:同一组方程的两种读法」——
①MSE 生来是几何量(SSE=|y−ŷ|²);②参数空间(碗/下山/碗底)与观测空间
(距离/ŷ 滑动/垂足)对照表,两幅画面逐帧对应;③恒等式:SSE 梯度分量
= −2(e·x)、−2(e·1),故梯度为零 ⇔ 残差垂直 ⇔ 正规方程,金句"学无可学之时,
恰是残差与模型互相垂直之刻";④推广到神经网络(平面变曲面,只剩下山)。
2.2 末尾补前向引用。ASCII 校验通过。未运行测试(纯文档改动)。

---

## 2026-07-24 18:45 · 新建 orthogonal-projection demo:图形化「最小二乘 = 正交投影」

**初始理解**:用户要一个专门的交互 demo,直接把"最小二乘求解就是对模型平面
求正交投影"这件事画出来,而不是像 least-squares 那样只在 README 文字里讲。
已确认(向用户提问)要新建独立 demo,而不是塞进现有 least-squares 页面。

**改动方案**:
- 新建 `demos/orthogonal-projection/`:用 p5 WEBGL 画抽象三维"预测空间"
  (3 个样本点的 y 打包成 ℝ³ 向量),模型 ŷ = k·x + b·1 张成一张半透明平面;
  滑块联动 k、b 让预测点在平面上滑动,残差线段颜色随"垂直程度"由灰变绿,
  另有固定的绿色目标点标出真正的正交投影(闭式解 k*、b*);按钮"跳到
  最优解""重置""换一组数据";orbitControl 拖拽旋转视角。
- 面板走既有 `shared/style.css` 配色(--a/--b/--sum/--proj/--lad),不改
  `MathViz`(3D 渲染是本 demo 专属能力,不算通用 2D 绘图,不并入公共层)。
  配色:x=蓝、1=紫、y=红、ŷ=黄、残差=灰→绿动态、目标点=绿色空心。
  数值面板不放 3D 文字(WEBGL 默认字体不可用),标签走面板图例。
- 首页新增卡片,放在"最小二乘"卡片之后。
- 完成后用 headless Chrome 截图验证 3D 场景与交互。

**设计决策(实现过程中发现并修正)**:
- 去掉了 `ambientLight`/`pointLight`:自定义 `beginShape/vertex` 画的模型
  平面没有显式法线,WEBGL 光照下渲染结果不可预期(可能整块偏暗),数学
  示意图不需要真实光照,改为纯色绘制,可预期且更清晰。
- k、b 滑块范围原计划按数据集手写区间,发现当区间跨度较大时,模型平面
  四角会离原点很远(千像素级),把 y、x、1 这些真正关心的向量挤成一团。
  改成由 buildData() 以最优解 (k*, b*) 为中心自动展开固定跨度
  (kSpan=1.5, bSpan=1.2),平面可视范围始终稳定围绕"标准答案",不随数据
  集变化而失控。
- 连带发现:换数据集后 k*、b* 会漂移,原来"重置到 (0,0)"的按钮在某些
  数据集(如第三组 k*=-1, b*=13/3)滑块范围根本不包含 0,重置会被浏览器
  的 min/max 强制钳位到边界,行为和按钮文案对不上。改为"重置到滑块区间
  起点 (kMin, bMin)",在任何数据集下都保证落在合法范围内。
- setup() 里只调用一次 `camera(500,-400,750,...)` 设定斜上方俯视的初始
  机位,draw() 里 orbitControl() 每帧接管——确认这是 p5 官方推荐写法
  (camera() 设初始姿态,orbitControl() 在其基础上响应拖拽/滚轮),不会
  每帧被重置。

**验证与遗留风险**:本轮无法使用 headless Chrome(用户中途选择跳过浏览器
扩展安装,截图/console 校验未能执行)。改为静态验证:`node --check`
通过 sketch.js 语法检查;起了本地 `python3 -m http.server` 用 curl 确认
index.html、sketch.js、README.md 及公共层资源均 200;逐行走查 p5 WEBGL
API 用法(orbitControl/sphere/beginShape/camera 等)与坐标缩放逻辑。
**没有做过真实浏览器里的可视化验收**,3D 场景的实际取景、滑块联动手感、
残差变色的观感是否符合预期仍未肉眼确认,建议用户本地打开
`demos/orthogonal-projection/index.html` 实际试一遍。

---

## 2026-07-25 按 todo.md 补充 orthogonal-projection/README.md

**初步理解**:用户要求阅读 `demos/orthogonal-projection/todo.md`(一份
构思笔记,记录了"y 打包成向量"、"5 个数据点"、"沿方向走长度构造 ŷ"、
"‖e‖²=SSE=欧氏距离"等几个教学点),并按这个思路"补充"——即把笔记里
还没进 README 的内容补进 `demos/orthogonal-projection/README.md`。

对照现有 README 发现:笔记第 4 点(SSE=距离平方)已在"三"节完整讲过;
第 1、2 点(y 是向量、模型平面)结论已有,但缺一个 3 维(可画)到 100 维
(纯抽象)之间"写得出数字但画不出来"的中间台阶;第 3 点("走两步"的向量
加法直觉,即沿 x 方向走 kx、再沿 1 方向走 b)README 里完全没有,直接甩
公式 ŷ=k(1,2,3)+b(1,1,1),缺一句更直觉的过渡。

**变更计划**(已通过 plan mode 确认):
1. "一、这个三维空间到底是什么"末尾补一段 5 维具体例子(y=(3,5,7,8,12),
   x=(1,2,3,4,5)),作为 3 维和抽象 100 维之间的台阶。
2. "二、为什么所有预测挤在一张平面里"给出公式前,先加一句"走两步"的
   向量加法直觉说明。
3. 清理 `demos/orthogonal-projection/todo.md`(构思稿已消化进 README,
   不是面向读者文档,按硬性约定 1 不保留平行文档)。
不改 sketch.js/index.html(5 维例子只是叙述性台阶,3D 画布仍固定 3 点)。

**总结**:待完成编辑后补充。

**已完成**:
- `demos/orthogonal-projection/README.md`:"一"节末尾新增 5 维具体例子
  段落(y=(3,5,7,8,12), x=(1,2,3,4,5)),作为 3 维可视化与抽象 100 维之间
  的认知台阶;"二"节公式前新增"走两步"向量加法直觉段落。
- 删除已消化完的构思稿 `demos/orthogonal-projection/todo.md`。
- 未修改 sketch.js / index.html。
- 未运行浏览器验证(纯文档改动,未启动本地服务器截图);仅人工通读改后
  文本确认前后衔接自然、公式渲染语法(KaTeX $$ ... $$)与文件其余部分一致。

---

## 2026-07-25(续)补充欧氏距离公式注解

**变更**:按用户要求,在 `demos/orthogonal-projection/README.md` 末尾新增
"注:什么是欧氏距离"一节,给出一般形式的欧氏距离公式
$\|\mathbf a-\mathbf b\|=\sqrt{\sum(a_i-b_i)^2}$,并在"三"节首次提到
"欧氏距离"处加一句指向文末注解的提示,同时把公式落回到 y、ŷ、SSE 的
具体对应关系上。未改代码文件。未做浏览器渲染验证,仅人工核对 KaTeX
公式语法与文件其余部分一致。

---

## 2026-07-26 让 orthogonal-projection 的 3D 图形更容易理解

**初步理解**:用户反馈正交投影 demo 的 3D 显示太抽象。headless 截图确认
五个问题:场景内零标注(全靠对照面板猜颜色)、默认机位差(平面近乎侧对
镜头呈细带状、场景偏出画布中心)、与熟悉的"散点+直线"视角断开、
"垂直最短"缺几何线索(无垂线/直角标记/斜边对比)、README 的"走两步"
直觉在图里无对应物。

**变更计划**(plan mode 已确认):
A. 画布左上角加 2D 小窗(原生 canvas):散点、当前直线、最优虚线、残差段,
   与滑块同步——建立"2D 直线 ↔ 3D 平面上一点"的对应。
B. 场景内标签改用 HTML 覆盖层(读 p5 内部 uMVMatrix/uPMatrix 做世界→屏幕
   投影;p5 已锁 1.9.4),支持中文,标 y/ŷ/垂足/x/1/模型平面/e/y₁y₂y₃。
C. 常驻直角三角形:y→垂足绿虚线(垂直残差)、垂足→ŷ 橙虚线(平面内误差)、
   y→ŷ 实线(斜边),低透明度填充 + 垂足直角标记;WEBGL 无 setLineDash,
   自写 drawDashedLine。
D. applyPreset 时自动取景(视线中心=原点/y/垂足质心,方向含平面法线分量),
   drawArrow 加圆锥箭头。
E. 面板加"显示合成路径 k·x + b·1"checkbox(默认开),画原点→k·x→ŷ 两段。
只动 demos/orthogonal-projection/ 三个文件,不动 shared 公共层,不引入
新库/字体。

**关键发现(实现过程中)**:
- 反复调机位无效后定量调试(?dbg 把关键点屏幕坐标写进 document.title,
  headless --dump-dom 读取)发现两个真正的问题:
  1) p5 的 orbitControl 以"世界 y 轴朝上"为前提工作,传给 camera() 的
     自定义 up 向量不可靠。解法:在 P() 里做基变换,把绘制坐标系整体旋转
     到与模型平面对齐(e1→x,e2→z,法线→-y),平面成为"水平地面",
     相机用普通斜俯视即可,orbitControl 的假设天然成立。
  2) 平面此前按 (k,b) 参数矩形绘制,而 x=(1,2,3) 与 1=(1,1,1) 只差约 22°,
     该矩形在平面上是剪切得极扁的斜条——这才是"细带"的根因,与机位无关。
     改为沿正交基 (e1,e2)、以垂足为中心的规整矩形补丁,大小恰好盖住滑块
     可达的所有 ŷ。
- 标签投影读 p5 内部 uMVMatrix/uPMatrix(在 orbitControl 之后、任何模型
  变换之前快照,此时 uMVMatrix 即纯视图矩阵),经截图确认与画面精确对齐。

**总结**:
- `sketch.js` 大改:2D 小窗、HTML 标签层、直角三角形(绿虚线垂线/橙虚线
  平面内误差/灰实线斜边+直角标记)、合成路径开关、圆锥箭头、平面对齐
  基变换、自动取景;保留 ?preset= 与 ?kb=、?dbg 调试参数。
- `index.html`:小窗 canvas + 标签层 + checkbox + 垂足数值行,demo 专属
  样式写在页内 <style>;hint 改为解释三角形。
- `README.md`:「如何使用」重写(先看小窗、标签、三角形、开关),"四"节
  补三角形的观察方式。
- 验证:node --check 通过;headless Chrome(--headless=new + swiftshader)
  截图验证:默认状态、最优状态(e·x/e·1 归零变绿、三角形隐藏)、
  preset 1/2、窄屏(640px)均正常;标签与场景对齐;结束后已停掉本地服务器。
- 遗留风险:交互手感(旋转、拖滑块的实时观感)未在真实浏览器里人工验证;
  标签投影依赖 p5 1.9.4 内部矩阵字段(CDN 已锁版本,升级 p5 时需回归)。

---

## 2026-07-26(续)重写根目录 README.md

**初步理解**:根 README 严重过时——只列了 dot-product 一个 demo,而项目现已有
4 个激活 demo(what-is-nn / dot-product / least-squares / orthogonal-projection)。
技术栈也不再单一:dot-product、least-squares 用 p5.js + MathViz(2D);
orthogonal-projection 用 p5.js WEBGL(3D,不走 MathViz);what-is-nn 是纯原生
canvas 的独立互动长文(不依赖 p5/KaTeX/MathViz)。线上站点已在
https://wanghaoming.github.io/math4llm/(各 demo README 已链此)。

**变更计划**:重写根 README,保持"面向开发者"定位——新增在线地址、4 个 demo
的列表(领域标签 + 一句话 + 串联脉络:点积埋投影伏笔 → 最小二乘回收 →
正交投影讲透 → 神经网络里点积是核心运算)、按渲染方式如实区分技术栈、
更新目录树、保留仍有效的 MathViz 模板与 API 速查并注明 3D/NN 为例外。
只改 README.md 一个文件。

**总结**:待完成后补充。

**已完成**:重写 README.md。新增在线体验地址;用表格列出 4 个已上线 demo
(领域 + 一句话 + 目录链接)与"规划中"的导数 demo;补一段串联脉络说明
(点积→最小二乘→正交投影→神经网络);技术栈按渲染方式分三类如实说明
(MathViz 2D / WEBGL 3D / 原生 canvas);更新目录树(补 README、CLAUDE/AGENTS、
标注 sketch.js 结构可不同);保留 MathViz 模板与 API 速查,并注明 3D/NN demo
可不套 MathViz。仅改 README.md 一个文件,未改代码,无需运行验证。

---

## 2026-07-26 · least-squares README 纠错

**初始理解**:用户要求审阅 `demos/least-squares/README.md` 找问题。逐行核对了
README 与 `sketch.js`、`index.html`,并交叉核对三个兄弟 demo 与本目录 `claude.md`。
共找出 20 项问题,其中硬性错误 5 项。

**关键发现**:
- A1 §四 中位数论证符号写反:同句内先说"左增右减",结论却写成 `(右 − 左)`。
- A2 §3.1 唯一的"碗"字符画画成了开口向下的山丘,`*`(标为"碗底")不在最低处。
- A3 "让 SAE 最小的直线一定恰好穿过至少两个点"表述过强(应为存在性),
  且"恰好"与"至少"自相矛盾;`sketch.js:146` 注释同款。
- A4 x 全相同时说成"最优不存在",实为参数不可辨识(解不唯一)。
- A5 残差与残差绝对值混用。
- B1 代码有 `PROBE_GROW = 1.3`(成功时步长放大),README 只讲减半,面板却会显示 δ 变大。
- C1 `orthogonal-projection/README.md:137` 声称本文推导过 XᵀXθ=Xᵀy 的矩阵形式,
  本文实际没有 —— 跨文档引用落空。

**变更计划**(用户确认范围:只修错,不做结构性扩写):
1. `README.md`:修 A1–A5、B1–B2、C2 链接、E1–E6 体例;§五 末尾新增 5.1 矩阵小节兑现 C1。
2. `sketch.js`:146 行注释同步改为存在性表述。
3. `index.html`:53 行死文案与 `sketch.js:278` 统一。
4. `claude2.md` 独有条目并入 `claude.md` 后删除该重复文件。

**设计决策**:
- "L1 基本无视离群点"按 correctness 做词级软化,但**保留**"紫线几乎不动"这类
  对 demo 现象的描述 —— 在这个 8 点场景里它确实不动,是准确观察而非过度断言。
- 新增的 5.1 矩阵小节顺带把"XᵀX 不可逆"与 A4 的 x 全相同退化情形挂钩,
  让 3.1 闭式解、A4 退化、§五 投影三处一次收拢。
- 本轮**不做**该目录 `claude.md` 里的扩写 backlog(闭式解入门解释、三点例子过渡、
  L2 梯度对比),留待后续单独一轮。

**总结**:待完成后补充。

**已完成**:

改动 4 个文件:
- `demos/least-squares/README.md` —— 修 A1(中位数符号)、A2(碗的字符画改为开口向上,
  五行散点对称于第 16 列)、A3(L1 两点性质改为存在性表述,并补一段说明"不是每条最优线
  都必须穿过两点")、A4(x 全相同改述为"解不唯一 / 参数不可辨识",§3.1、§3.2、§七 三处
  同步)、A5(残差 → 残差绝对值);B1(§6.1 与 §如何使用 补 PROBE_GROW 的步长放大行为)、
  B2(补删点下限 2);C2(§五 开头加指向 orthogonal-projection demo 的链接);
  E1–E4、E6(全角标点全部转半角、"的/地"、blockquote 空格、"三个按钮"列表拆出第 6 项、
  代码块补 text 标记);D1 词级软化(表格"基本无视"→"受影响远小于 L2",并补一段
  "不是不受影响,而是不被单个离群点支配");**新增 §5.1 矩阵形式**(设计矩阵 X、θ、
  ŷ=Xθ、Xᵀ(y−Xθ)=0、XᵀXθ=Xᵀy、θ=(XᵀX)⁻¹Xᵀy),兑现 C1。
- `demos/least-squares/sketch.js` —— `bestFitL1()` 上方注释同步改为存在性表述,
  并把"无解"改为"k、b 无法确定"。仅注释,无逻辑改动。
- `demos/least-squares/index.html` —— steplog 初始文案与 sketch.js 统一。
- `demos/least-squares/claude.md` —— 并入 claude2.md 独有的 6 条建议,删除 claude2.md。

**验证**:
- `node --check sketch.js` 通过。
- `python3 -m http.server` 起服务,页面 HTTP 200,serve 出的 HTML 含更新后文案。
- 字符画列位用 awk 核对:五行散点分别在列 (4,28)(6,26)(9,23)(13,19) 与 `*`@16,
  中心全部为 16,开口向上。
- `grep '[，:;?!()]'` 确认全角标点已清零;`$$` 共 20 个(10 组配平)。
- **未做**:浏览器实际渲染与交互验证 —— Chrome 扩展未连接,无法启动浏览器。
  §5.1 新增的 bmatrix 公式在 GitHub/KaTeX 下的渲染效果未经肉眼确认。
- **未做(本轮范围外)**:claude.md 里的扩写 backlog(闭式解入门解释、三点例子过渡到
  n 维、L2 梯度对比 dL/de=2e vs ±1)。

---

## 2026-07-27 · least-squares README 按 claude.md 扩写

**初始理解**:落地上一轮(commit d9c7049)明确跳过的第二批——`demos/least-squares/claude.md`
里的教学扩写要求。

**关键发现**:claude.md 里分量最重的两块「必须加强:正交投影」和「必须补充:模型空间的
真正意义」,**已被 `demos/orthogonal-projection/README.md` 完整实现**,且用的是 claude.md
指定的同一组例子((1,2)(2,3)(3,5)、y=(2,3,5)、y=x+1→(2,3,4)、"平面不是拟合直线"、
100 样本、地面垂线类比、欧氏距离注解)。claude.md 显然写于该 demo 之前。照搬会造成约
100 行跨文档重复。

**设计决策**:
- 该块改为写约 20 行三点跳板 + 交棒给兄弟 demo,不照搬(用户确认)。
- 章节顺序按用户指定:试探法 → §五,正交投影 → §六(即现有 §五、§六 对调),
  而非 claude.md 建议的"试探法排在闭式解之前"的完整 7 阶段重排。
- N3(L2 梯度对比)放在新 §五 讲完导数之后,概念衔接最顺,而不是放 §二。
- N6(梯度=0 ⟺ 残差垂直)、N7(参数空间 vs 预测空间)放 §六 末尾,
  满足 claude.md 要求的前置条件,并把 §3.1 那只碗和 §六 那张平面焊在一起。

**变更计划**:仅改 `README.md`。章节重排 + 5 处交叉引用修正 + 10 项新增内容
(N1 闭式解是什么、N2 有闭式解为何还要试、N3 两种损失的下坡信号、N4 三点跳板、
N5 模型空间意义、N6 梯度=0⟺残差垂直、N7 参数/预测空间、N8 神经网络表述加谨慎、
N9 残余绝对化表述、N10 全文知识链收束)。不动 sketch.js / index.html /
orthogonal-projection README。

**总结**:待完成后补充。

**已完成**:

仅改 `demos/least-squares/README.md`(406 → 544 行,+34%),未动代码。

章节重排:§五(正交投影)与 §六(试探法)对调,现为 §五 试探法(5.1–5.4)、
§六 正交投影(6.1–6.6)。同步修正 5 处交叉引用(第五节→第六节 共 3 处、
5.1 节→6.4 节、试探法节尾的"第五节已回答过"改为前瞻"紧接着的第六节")。
另把 §六 里原本无编号的三个小节补上 6.1/6.2/6.3,与全文体例统一。

新增内容(对应 claude.md):
- N1 §3.1 开头补"闭式解是什么"(2x+3=11 → x=4)与闭式解/数值优化两条路线对照表;
- N2 §五 开头补"有闭式解为什么还要一步步试"(神经网络通常没有闭式解);
- N3 新增 5.3「两种损失给的下坡信号不一样」:dL/de = 2e vs ±1,并按 claude.md
  要求加平衡表述"别读成 L2 永远更好";
- N4 新增 6.1 三点跳板((1,2)(2,3)(3,5) → y=(2,3,5) → ŷ=k(1,2,3)+b(1,1,1)),
  含 claude.md 点名的"这张平面不是散点图里那条拟合直线";详解交棒 orthogonal-projection;
- N5 6.2 补模型空间意义(100 样本 / 只占极小一部分 / 拟合的本质那句);
- N6 新增 6.5「梯度为零 = 残差垂直」:∂SSE/∂k = −2(e·x)、∂SSE/∂b = −2(e·1);
- N7 新增 6.6「两个空间,同一个最优解」:碗底与垂足,把 §3.1 与 §六 焊在一起;
- N8 §5.4 与 §八 按 claude.md 补 6 加谨慎表述(非线性模型整体正交不再成立、
  复杂网络无实用闭式解);"线性回归就是最简单的神经网络"改为"可以看成";
- N9 软化残余绝对化表述(开篇"纹丝不动"→"几乎不动";§七"一人一票"→"人人推力相同");
- N10 §八 开头补全文知识链与核心那句话。

**刻意未做**:未照搬 claude.md 的正交投影/模型空间完整展开(已由
orthogonal-projection/README.md 实现,照搬会造成约 100 行跨文档重复);
未按 claude.md 建议把试探法排到闭式解之前(用户指定排 §五)。

**验证**:
- 章节编号连续、子节号与父节号一致(3.1–3.2 / 5.1–5.4 / 6.1–6.6)。
- 交叉引用逐条核对,14 处全部指向正确章节。
- 全角标点 grep 为空;`$$` 32 个(16 组配平)。
- 本地 server:index.html 与 sketch.js 均 200,代码未受影响。
- 手工复核新增公式:∂SSE/∂k = −2(e·x)、∂SSE/∂b = −2(e·1)、
  ŷ=(k+b,2k+b,3k+b)=k(1,2,3)+b(1,1,1)、y=x+1→(2,3,4)、y=2x→(2,4,6) 均正确。
- **未做**:浏览器实际渲染验证(Chrome 扩展未连接),新增公式在 GitHub/KaTeX 下的
  显示效果仍未经肉眼确认。

---

## 2026-07-27 · 精简「如何使用」,消除与 §5.1 的重复

**初始理解**:用户问「如何使用」是否该整节移进 §五 当第一小节。

**判断**:不整节移。「如何使用」6 条里只有第 5、6 条属于 §五 话题;前 4 条是全文前置
(§一 第 64 行就要求"勾选显示残差竖线"、§二 要求"勾选显示残差的平方"),搬走会让前置
信息落到正文中段(第 242 行 / 全文 544 行),且打破全项目体例(dot-product:6、
orthogonal-projection:10 的「如何使用」都紧跟导语)。

但用户戳中了真问题:第 5、6 条与 §5.1 有约 12 行概念重复,其中**面板七项逐字相同**
(30–32 行 vs 273–274 行),步长规则与四方向机制也各讲了两遍。用户确认采用"拆"的方案。

**设计决策**:「如何使用」只留**控件事实**(按钮叫什么、按了有什么反应),
**机制解释**全部归 §五。特别注意:按钮名、"拖动清空步数"、"收敛后变灰"这三项
§5.1 里**没有**,不能一并删掉——它们是控件说明,只有「如何使用」讲。

**变更**:仅改 `demos/least-squares/README.md` 第 25–38 行,§五 一字未动。
删去四方向试探机制、面板七项、步长放大/减半规则(均已由 §5.1 完整覆盖),
改为两处"见第五节"的指路。14 行 → 11 行。

**验证**:
- 删除项逐条在 §5.1(254–271 行)找到对应,无信息丢失;
- 保留项(重置数据 / 清空步数 / 自动变灰)grep 确认仍在;
- 机制描述现在全文只剩 §五 一处(254、264、267、270–271、280 行);
- 全角标点 grep 为空。
- 未做:浏览器渲染验证(Chrome 扩展仍未连接)。
