## 三、几何意义:最小二乘 = 一次投影

这是「点积」demo 结尾埋的伏笔,现在回收。换一副眼镜看同一个问题:
不再把数据看成"平面上的 n 个点",而是把所有观测值**竖着堆成一个向量**:

$$\mathbf{y} = (y_1, y_2, \ldots, y_n) \qquad \text{(n 维空间里的一个箭头)}$$

直线能产生的预测也是一个 n 维向量,而且只有两个自由度:

$$\hat{\mathbf{y}} = k \cdot (x_1, \ldots, x_n) + b \cdot (1, \ldots, 1)$$

k 和 b 随便取,所有可能的预测 ŷ 张成 n 维空间里的一张**二维平面**
(模型平面)。真实观测 y 因为带噪声,一般**不在**这张平面上。
于是"找最好的直线"变成了一个纯几何问题:

> **在模型平面上,找离 y 最近的那个点。**

答案你在「点积」demo 里已经见过:**把 y 垂直投影到平面上**。

```
              * y    (观测向量, n 维空间中一点)
             /.
            / .
           /  .      <- 残差向量 e = y - yhat, 垂直于平面
          /   .
         /    .
   -----+-----+----------------
        O     yhat   (y 在模型平面上的投影 = 最小二乘的预测)
```

"最近" = 残差向量 e 最短,而 SSE 恰好就是 |e|²(平方和 = 长度的平方,
「点积」demo 3.4 的副产品 a·a = |a|²)。垂直投影最短,所以:

> **最小二乘的解 = y 在模型平面上的投影;残差向量 ⊥ 模型平面。**

"垂直"翻译成代数,就是两个点积为零(e 和平面的两个方向都零贡献——
「点积」demo 2.2 那个"垂直 = 互不贡献"):

$$\mathbf{e} \cdot (x_1, \ldots, x_n) = 0 \qquad \mathbf{e} \cdot (1, \ldots, 1) = 0$$

把这两个点积展开,得到的正是 2.1 节的闭式解——教科书管它叫**正规方程**。

这段话其实是在讲**最小二乘法最本质的几何意义**。如果第一次看，很容易被"n维空间""模型平面""投影"这些词吓住。

其实它的逻辑只有四步，我们一点一点来看。

---

# 第一步：以前我们怎么看问题？

以前学最小二乘法，我们画的是这种图。

```
y
↑
│            ●
│      ●
│   ●
│        ●
│______________→ x
```

这里每个点都是

```
(x,y)
```

然后找一条直线

```
y=kx+b
```

让这些点距离直线最近。

这是二维平面里的理解。

---

# 第二步：现在换一种看法

作者说：

> 不看成 n 个点。

什么意思？

假设有四个数据

| x | y |
| - | - |
| 1 | 2 |
| 2 | 3 |
| 3 | 5 |
| 4 | 4 |

以前认为有四个点。

现在不要这么看。

而是只看

所有的 y。

把它们竖起来。

```
      2
y =   3
      5
      4
```

数学写成

[
y=
\begin{pmatrix}
2\
3\
5\
4
\end{pmatrix}
]

它不是四个数。

它是

**四维空间里的一个向量。**

可以想象成

```
二维：

→

三维：

↗

四维：

一个你画不出来的箭头
```

所以

作者说

> y 是 n维空间里的一个箭头。

---

# 第三步：预测值也是一个箭头

假设我们猜

```
y=x+1
```

那么预测

```
2
3
4
5
```

于是预测值

[
\hat y=
\begin{pmatrix}
2\
3\
4\
5
\end{pmatrix}
]

也是一个四维向量。

于是

真实值

```
2
3
5
4
```

预测值

```
2
3
4
5
```

都变成了

四维空间里的两个箭头。

注意：

以前比较的是

```
点 到 直线
```

现在比较的是

```
一个向量 到 另一个向量
```

---

# 第四步：为什么说所有预测值组成一个平面？

这是最关键的一步。

模型

[
\hat y=kx+b
]

展开以后

[
\hat y=
k
\begin{pmatrix}
x_1\
x_2\
x_3\
x_4
\end{pmatrix}
+
b
\begin{pmatrix}
1\
1\
1\
1
\end{pmatrix}
]

注意这两个向量：

```
x方向

1
2
3
4
```

和

```
常数方向

1
1
1
1
```

任何预测

都是

```
k×第一根箭头

+

b×第二根箭头
```

例如

```
2×

1
2
3
4

+

3×

1
1
1
1
```

或者

```
5×

1
2
3
4

-

2×

1
1
1
1
```

永远都是

这两个向量的线性组合。

---

如果学过线性代数，

你知道

两个方向

可以张成一个平面。

二维里：

```
↗

↘

所有组合

形成

一个平面
```

虽然现在是在四维空间，

但是原理完全一样。

于是

所有可能预测值

全部落在同一张

**二维平面**

上。

作者称它

```
模型平面
```

---

# 第五步：真实数据为什么不在平面上？

真实数据

```
2
3
5
4
```

通常带噪声。

例如真正规律可能是

```
2
3
4
5
```

只是第三个点高一点。

第四个低一点。

所以

真实向量

一般不会

刚好落在模型平面。

它在平面外。

就像

```
          y
          ●

──────────────

模型平面
```

---

# 第六步：那怎么办？

以前我们说

找一条最好的直线。

现在变成

```
找平面上

离 y 最近

那个点。
```

就是

```
          y
          ●
          │
          │
          │
──────────┼────────

         yhat
```

这个

```
yhat
```

就是

预测值。

---

# 第七步：为什么一定是垂直？

这是初中几何。

你站在天花板上。

想掉到地板。

有很多路线。

```
斜着

＼

竖着

│
```

哪条最短？

一定是

```
│
```

垂直。

所以

```
距离最短

↓

一定垂直。
```

---

因此

残差

```
e

=

y

-

yhat
```

就是

```
          y
          ●
          │
          │ e
          │
──────────●──────

         yhat
```

它一定垂直于整个模型平面。

---

# 第八步：为什么突然出现点积？

你之前学过：

> 两个向量垂直 ⇔ 点积等于0。

genui{"linear_algebra_optimization_learning_block":{"type_id":"VECTOR_PROJECTION","locale_override":"zh-CN"}}

于是

残差

和

平面里的任何方向

都应该垂直。

平面只有两个基础方向

```
x方向

1方向
```

所以只需要验证

```
e·x=0

e·1=0
```

因为

如果一个向量

同时垂直于

平面的两个独立方向

那它一定垂直整个平面。

---

# 第九步：为什么这就是正规方程？

把

```
e=y-yhat
```

代进去。

例如

第一条

```
e·x=0
```

变成

```
(y-yhat)·x=0
```

把

```
yhat=kx+b1
```

继续代进去。

展开。

整理。

最后得到

```
关于k,b

两个方程
```

这就是教科书里的

**正规方程（Normal Equation）**。

所以你会发现：

> **正规方程并不是凭空推导出来的。**

它其实只是把一句非常简单的话：

> **"残差必须垂直于模型平面。"**

翻译成了数学语言。

---

## 整个逻辑可以浓缩成一句话

传统思维：

> 在二维平面中，找一条距离所有数据点最近的直线。

线性代数思维：

> 把所有观测值看成一个 **n 维向量**，把所有可能的预测值看成 **n 维空间中的一个二维平面**。最小二乘法就是把真实向量 **正交投影** 到这个模型平面上。

所以，你之前学过的几个知识点其实在这里全部串起来了：

* **向量长度**：(|e|^2=e\cdot e=\text{SSE})（平方误差和就是残差向量长度的平方）。
* **点积**：点积为 0 表示两个向量垂直。
* **向量投影**：距离平面最近的点一定是正交投影。
* **最小二乘法**：就是这个正交投影在机器学习中的名字。
* **正规方程**：就是把"残差与模型平面正交"展开后得到的代数方程。

很多人学完点积之后觉得它和机器学习没什么关系，其实**最小二乘法就是点积和向量投影最经典、最重要的应用之一**。理解了这一层，再学习梯度下降、PCA、支持向量机等算法时，会发现它们几乎都建立在同样的几何思想之上。
