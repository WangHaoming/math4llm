## 目标

修改 `demos/least-squares/README.md` 时，优先保持当前文章“直觉优先、可视化优先、循序渐进”的风格，不要把它改成传统教材。

核心目标是：

1. 让没有高等数学基础的读者理解最小二乘为什么存在。

2. 让读者真正理解 L1、L2、残差、拟合、模型空间、正交投影、闭式解之间的关系。

3. 最终自然衔接到导数、梯度下降和神经网络训练。

4. 在保持直观性的同时，提高数学表述的严谨性。

---

## 当前文章做得好的地方

这些部分尽量保留，不要大幅重写。

### 1. 从“什么叫最好”开始

不要一上来直接给出：

\\[

\\min_{k,b}\\sum_i(y_i-kx_i-b)^2

\\]

当前文章先从“哪条线最好”出发，再引出残差和损失函数，这个教学顺序很好。

应该继续保持：

> 问题 → 直觉 → 可视化 → 数学表达

而不是：

> 定义 → 公式 → 推导

---

### 2. L1 和 L2 的可视化对比

保留“长度 vs 面积”的直觉：

- L1：残差绝对值，可理解为长度

- L2：残差平方，可理解为正方形面积

尤其要保留离群点拖动实验。

这个实验可以非常直观地说明：

\\[

e\\rightarrow 2e

\\]

对于 L1：

\\[

|e|\\rightarrow 2|e|

\\]

对于 L2：

\\[

e^2\\rightarrow 4e^2

\\]

重点应表达为：

> L2 会更强烈地惩罚大误差，因此更容易受到离群点影响。

---

### 3. 从二维散点切换到高维向量

这是整篇文章最有价值的部分之一，必须保留。

继续使用：

\\[

\\mathbf y=(y_1,y_2,\\ldots,y_n)

\\]

以及：

\\[

\\hat{\\mathbf y}

=

k(x_1,\\ldots,x_n)

+

b(1,\\ldots,1)

\\]

把“找一条最合适的直线”重新解释为：

> 在模型能够产生的所有预测结果中，找到离真实数据最近的那个。

这个思路是后续理解线性代数、机器学习和神经网络的重要桥梁。

---

### 4. 试探法 → 导数 → 梯度下降

当前用“尝试修改 k 和 b，看 loss 是否下降”的方式引出优化，非常适合初学者。

建议继续保持：

> 暴力试探  

> ↓  

> 太慢  

> ↓  

> 需要知道往哪个方向走  

> ↓  

> 导数  

> ↓  

> 梯度下降

不要直接从最小二乘跳到求导公式。

---

# 需要修改和加强的部分

## 1. 修正关于 L1 离群点的表述

不要写成：

> L1 基本无视离群点

或者：

> L1 是一人一票，所以离群点不会影响结果

这种说法过于绝对。

应该改成：

> L1 对大误差的惩罚只按线性增长，因此相比 L2，更不容易让单个极端离群点支配整体损失。

可以补充：

\\[

L_1=|e|

\\]

当误差从 10 增大到 100：

\\[

10\\rightarrow100

\\]

损失扩大 10 倍。

而 L2：

\\[

L_2=e^2

\\]

则：

\\[

100\\rightarrow10000

\\]

损失扩大 100 倍。

重点是“更稳健”，不是“完全不受影响”。

---

## 2. 修正“L1 没有闭式解”的说法

不要直接写：

> L1 没有闭式解。

这样容易误导。

建议写成：

> 对一般的多参数 LAD / L1 回归，没有像普通最小二乘那样简单统一的闭式公式，通常需要通过线性规划或其他数值优化方法求解。

同时可以指出一个简单反例：

对于：

\\[

\\min_c\\sum_i|y_i-c|

\\]

最优解是数据的中位数。

因此不能把“L1”与“没有闭式解”直接画等号。

---

## 3. 谨慎处理“L1 最优线一定经过两个点”

如果文章中保留类似：

> 最优的 L1 直线一定经过至少两个数据点

需要加限制条件。

建议改成：

> 在一般非退化情形下，可以找到一个最优 LAD 解，使其经过足够数量的数据点。因此在这个二维教学 demo 中，可以通过枚举点对来构造候选解。

不要把 demo 中方便使用的性质写成无条件成立的数学定理。

---

# 必须加强：闭式解的解释

当前“闭式解”这个概念对于初学者太突然。

建议先加入一个非常简单的定义。

## 闭式解是什么

先使用：

\\[

2x+3=11

\\]

得到：

\\[

x=4

\\]

解释：

> 如果我们能够通过一个明确公式，直接从已知条件算出答案，而不需要不停尝试，这类结果可以粗略理解为闭式解。

然后对比“迭代求解”。

### 闭式解

\\[

\\text{数据}

\\rightarrow

\\text{公式}

\\rightarrow

\\text{最优答案}

\\]

### 数值优化

\\[

\\text{初始值}

\\rightarrow

\\text{修改}

\\rightarrow

\\text{修改}

\\rightarrow

\\text{修改}

\\rightarrow

\\text{接近最优答案}

\\]

然后说明普通最小二乘的特殊之处：

> 它既可以被当成一个优化问题，也可以直接推导出解析公式。

---

# 必须加强：正交投影

这是当前文章中最容易让初学者失去理解的一部分。

不要直接从二维散点跳到：

> 高维空间里的正交投影

中间必须增加完整过渡。

---

## 建议使用 3 个数据点讲解

例如：

\\[

(1,2),(2,3),(3,5)

\\]

真实 y 为：

\\[

\\mathbf y=(2,3,5)

\\]

解释：

> 不再把 2、3、5 看成三个分散的数字，而是把它们打包成一个三维向量。

三个维度分别代表：

- 第 1 个样本的 y

- 第 2 个样本的 y

- 第 3 个样本的 y

---

## 每条直线都对应一个预测向量

例如：

\\[

y=x+1

\\]

得到：

\\[

\\hat{\\mathbf y}=(2,3,4)

\\]

再比如：

\\[

y=2x

\\]

得到：

\\[

\\hat{\\mathbf y}=(2,4,6)

\\]

强调：

> 每选择一组 k、b，就会产生一个完整的预测向量。

---

## 解释为什么所有预测形成一个平面

固定：

\\[

x=(1,2,3)

\\]

则：

\\[

\\hat{\\mathbf y}

=

(k+b,2k+b,3k+b)

\\]

可以拆成：

\\[

\\hat{\\mathbf y}

=

k(1,2,3)+b(1,1,1)

\\]

因为只有两个可自由变化的参数：

\\[

k,b

\\]

所以所有可能预测都由两个方向：

\\[

(1,2,3)

\\]

和：

\\[

(1,1,1)

\\]

组合而成。

因此它们形成一个二维平面。

这里需要明确告诉读者：

> 这个平面不是原始 x-y 坐标图中的那条拟合直线，而是在“所有预测结果组成的空间”中的一个平面。

这是最关键的区别。

---

## 从平方误差连接到距离

真实数据：

\\[

\\mathbf y

\\]

预测：

\\[

\\hat{\\mathbf y}

\\]

误差：

\\[

\\mathbf e

=

\\mathbf y-\\hat{\\mathbf y}

\\]

向量长度：

\\[

\\|\\mathbf e\\|

=

\\sqrt{

e_1^2+e_2^2+\\cdots+e_n^2

}

\\]

因此：

\\[

\\|\\mathbf e\\|^2

=

e_1^2+e_2^2+\\cdots+e_n^2

\\]

也就是：

\\[

\\sum_i(y_i-\\hat y_i)^2

\\]

因此必须明确写出：

> 最小化平方误差，其实就是让真实数据向量和预测向量之间的欧氏距离最小。

---

## 再解释正交投影

不要先给定义。

先使用“点到平面的最短距离是垂线”这个直觉。

可以类比：

> 一个悬在地面上方的点，垂直落到地面的那个点，是地面上离它最近的点。

然后翻译回最小二乘：

- 真实数据 \\(\\mathbf y\\) 不一定在模型空间里

- 所有可实现预测 \\(\\hat{\\mathbf y}\\) 都在模型空间中

- 我们要找距离 \\(\\mathbf y\\) 最近的 \\(\\hat{\\mathbf y}\\)

- 最近点由垂线得到

- 这个最近点就是正交投影

最终总结：

\\[

\\boxed{

\\text{最小二乘拟合}

=

\\text{把真实数据向量投影到模型能够表达的空间}

}

\\]

---

# 必须补充：模型空间的真正意义

当前文章应该更明确解释“模型空间”到底是什么。

可以这样说：

假设有 100 个样本：

\\[

\\mathbf y=(y_1,\\ldots,y_{100})

\\]

这是一个 100 维向量。

但是线性模型：

\\[

y=kx+b

\\]

只有两个自由参数：

\\[

k,b

\\]

因此模型并不能产生任意一个 100 维向量。

它只能产生：

\\[

\\hat{\\mathbf y}

=

k\\mathbf x+b\\mathbf 1

\\]

这意味着：

> 模型能够表达的答案，只占整个 100 维空间中的一个很小部分。

这就是模型空间。

建议突出一句：

> 拟合的本质不是“画一条靠近散点的线”，而是“在模型允许表达的所有答案中，寻找离真实答案最近的那个”。

---

# 建议补充：从二维直线推广到矩阵

在读者理解正交投影后，再引入矩阵，不要提前。

定义：

\\[

X=

\\begin{bmatrix}

x_1&1\\\\

x_2&1\\\\

\\vdots&\\vdots\\\\

x_n&1

\\end{bmatrix}

\\]

以及：

\\[

\\theta=

\\begin{bmatrix}

k\\\\

b

\\end{bmatrix}

\\]

得到：

\\[

\\hat y=X\\theta

\\]

最小二乘写成：

\\[

\\min_\\theta\\|y-X\\theta\\|^2

\\]

再利用“最优时残差垂直于模型空间”：

\\[

X^T(y-X\\theta)=0

\\]

从而：

\\[

X^TX\\theta=X^Ty

\\]

若 \\(X^TX\\) 可逆：

\\[

\\theta=(X^TX)^{-1}X^Ty

\\]

这里需要强调：

> 正规方程不是突然冒出来的公式，它只是“残差与模型空间正交”这一几何事实的代数表达。

---

# 建议加强：为什么 L2 适合优化

文章目前只强调“平方误差好算”，还不够。

建议补充梯度层面的区别。

对于：

\\[

L=e^2

\\]

有：

\\[

\\frac{dL}{de}=2e

\\]

因此：

> 误差越大，产生的梯度也越大。

对于：

\\[

L=|e|

\\]

在 \\(e\\neq0\\) 时：

\\[

\\frac{dL}{de}

=

\\begin{cases}

1,&e>0\\\\

-1,&e<0

\\end{cases}

\\]

说明：

> L1 对大误差和小误差提供的梯度幅度基本相同，而 L2 会随着误差增大而产生更强的修正信号。

同时提醒：

> L2 对离群点更敏感，这既可能是优点，也可能是缺点。

不要把 L2 描述成“永远更好”。

---

# 最终教学路线建议

建议 README 最终按照下面的知识链组织。

## 第一阶段：什么是拟合

散点  

↓  

选择模型  

↓  

模型产生预测  

↓  

预测和真实值之间产生残差

---

## 第二阶段：怎么衡量误差

直接求和会正负抵消  

↓  

L1  

↓  

L2  

↓  

比较两者对离群点的不同反应

---

## 第三阶段：什么叫最优

定义 loss  

↓  

让 loss 最小  

↓  

先用暴力试探理解“优化”

---

## 第四阶段：闭式解

解释“直接算”与“慢慢找”的区别  

↓  

说明普通最小二乘恰好可以直接求解

---

## 第五阶段：高维几何

把所有真实 y 打包成向量  

↓  

把每个模型预测也打包成向量  

↓  

所有模型预测形成模型空间  

↓  

平方误差等于向量距离平方  

↓  

最小二乘 = 找模型空间中最近的点  

↓  

最近点 = 正交投影

---

## 第六阶段：矩阵表达

\\[

\\hat y=X\\theta

\\]

↓  

\\[

\\min_\\theta\\|y-X\\theta\\|^2

\\]

↓  

残差与模型空间正交

\\[

X^T(y-X\\theta)=0

\\]

↓  

正规方程

---

## 第七阶段：走向神经网络

从：

\\[

\\hat y=kx+b

\\]

推广到：

\\[

\\hat y=f_\\theta(x)

\\]

从：

\\[

\\min_{k,b}E(k,b)

\\]

推广到：

\\[

\\min_\\theta L(\\theta)

\\]

强调：

> 最小二乘只是一个非常简单的机器学习优化问题。神经网络训练做的事情本质上类似：寻找一组参数，让损失函数尽可能小。

但对于复杂神经网络，通常没有像普通最小二乘这样方便的闭式解，因此需要：

\\[

\\text{导数}

\\rightarrow

\\text{梯度}

\\rightarrow

\\text{梯度下降}

\\rightarrow

\\text{反向传播}

\\]

---

# 整篇文章应该传达的核心思想

最终不要让读者只记住：

\\[

\\sum_i(y_i-\\hat y_i)^2

\\]

而应该让读者理解下面几件事其实是一条完整链条：

\\[

\\boxed{

\\text{残差}

\\rightarrow

\\text{损失函数}

\\rightarrow

\\text{距离}

\\rightarrow

\\text{模型空间}

\\rightarrow

\\text{正交投影}

\\rightarrow

\\text{最优解}

\\rightarrow

\\text{导数}

\\rightarrow

\\text{梯度下降}

}

\\]

最重要的一句话：

> 最小二乘的本质，不只是“让几个误差的平方之和最小”，而是在模型能够表达的所有答案中，寻找距离真实答案最近的那个答案。

进一步连接机器学习：

> 机器学习训练，本质上也是在模型能够表达的巨大参数空间中，寻找一个尽可能好的答案。

---

# 补充建议（自 claude2.md 合并）

以下条目原本单独存放在 `claude2.md`。两份文件内容高度重复，现已合并至本文件，
`claude2.md` 已删除。以下是 `claude2.md` 中本文件原先没有覆盖的部分。

## 补 1. 保留残差平方的面积可视化

继续保留：

- 残差 = 竖直距离
- 残差平方 = 黄色正方形面积
- SSE = 所有黄色面积之和

这个设计非常适合解释：误差从 \(e\) 变成 \(2e\) 时，\(e^2\) 变成 \(4e^2\)。

从而让读者直观看到：

> L2 会更加重罚大误差。

---

## 补 2. 保留“闭式解 vs 梯度下降”两条路线

这是文章非常重要的结构，应该继续明确表现：

### 闭式解

\[
\text{数据}\rightarrow\text{公式}\rightarrow\text{直接得到最优参数}
\]

### 梯度下降

\[
\text{初始参数}\rightarrow\text{不断修改}\rightarrow\text{不断降低 loss}\rightarrow\text{接近最优参数}
\]

但需要在正文中先解释“闭式解”这个词本身，否则初学者很容易直接略过。

---

## 补 3. 明确说明为什么有闭式解还要讲梯度下降

建议增加一句核心解释：

> 如果目标只是拟合这一条直线，直接用闭式解最快。这里仍然演示梯度下降，是因为
> 真正的神经网络通常没有这么方便的闭式解，而“不断调整参数让 loss 下降”才是
> 机器学习训练中更普遍的情况。

这样可以避免读者产生“既然能直接算出来，为什么还要下山？”的疑问。

---

## 补 4. 参数空间和预测空间要区分得更简单

建议先给一句最简单的版本：

> \(k,b\) 是你手里可以调的旋钮；\(\hat{\mathbf y}\) 是调完旋钮以后模型真正给出的答案。

然后再说：

- 参数空间里移动的是 \((k,b)\)
- 预测空间里移动的是 \(\hat{\mathbf y}\)

二者一一对应。因此梯度下降可以有两幅图：

- **参数空间**：\((k,b)\) 在 SSE 的碗上向最低点移动。
- **预测空间**：\(\hat{\mathbf y}\) 在模型平面里向 \(\mathbf y\) 的最近点移动。

最后：

> 碗底和垂足，其实是同一个最优解在两个空间里的不同表现。

---

## 补 5. “梯度 = 0”和“残差垂直”的关系要保留，但放晚一点

当前恒等式非常重要，应该保留：

\[
\frac{\partial\,\mathrm{SSE}}{\partial k}=-2(\mathbf e\cdot\mathbf x),
\qquad
\frac{\partial\,\mathrm{SSE}}{\partial b}=-2(\mathbf e\cdot\mathbf 1)
\]

但要保证读者已经先理解：

1. 什么是模型空间
2. 什么是预测向量
3. 为什么 SSE 是距离平方
4. 为什么最近点对应垂直

然后再告诉他：

> 更神奇的是，微积分最后得到的“梯度为零”，刚好就是几何里的“残差和模型方向垂直”。

即 \(\text{梯度}=0\) 等价于 \(\mathbf e\cdot\mathbf x=0\) 且 \(\mathbf e\cdot\mathbf 1=0\)，
所以 \(\mathbf e\perp\text{模型空间}\)。这是全文非常漂亮的收束点。

---

## 补 6. 神经网络部分需要稍微谨慎

不要写成：

> 套上激活函数后，只剩梯度下降这一条路

这种表述太绝对。建议改成：

> 对复杂神经网络，一般不存在像线性最小二乘这样简单、实用的闭式解，因此实际训练
> 通常依赖基于梯度的数值优化方法。

另外“收敛时残差依旧垂直于这个集合的切方向”这个表述对一般非线性网络需要谨慎。
更安全的说法：

> 在线性最小二乘中，最优残差与整个模型子空间正交。推广到非线性模型时，可以把
> 模型的可达预测看成一张弯曲曲面；在满足适当条件的局部最优点附近，损失对可行
> 参数方向的一阶变化为零。

不要让读者误以为深度网络训练永远等价于全局意义上的“正交投影到曲面”。
