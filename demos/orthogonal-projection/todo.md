y 的真实值的所有值，构成一个向量

比如有 5 个真实值（即 5 个数据点），那么 y 就是一个 5 维的向量

例如：
（3,5,7,8,12）

那么 x 同样也有 5 个值
（1,2,3,4,5）



x 和 （1,1,1,1，1）就构成了一个模型平面

所有的预测值，只能在这个模型平面上，英文预测值的计算公式是 y^ = kx + b

从向量的角度理解预测值，需要沿 x 方向走 kx 的长度，在沿 （1,1,1,1,1）的方向走 b 的长度，就会得到一个 5 维向量，也就预测值的向量。

那么想让预测值和真实值

‖e‖²(= SSE) 最小， 也就是 y 和 y^ 的欧式距离最小

因为  y 和 y^ 的欧氏距离是  


